type Query {
  signin(email: String!, password: String!): String!
  user(userId: ID): User
  book(bookId: String!): Book
  bookQuery(query: String!): [Book!]!
}

type Mutation {
  signup(user: UserInput!): String!
  addFeedBook(bookId: String!, comment: String!, date: String!): UserFeedBook!
}

type User {
  _id: ID!
  email: String!
  name: String!
  feedBooks(bookId: String): [UserFeedBook!]!
}

type UserFeedBook {
  bookId: String!
  book: Book!
  comment: String!
  date: String!
}

input UserInput {
  email: String!
  name: String!
  password: String!
}

type Book {
  bookId: String!
  title: String!
  subTitle: String
  description: String
  authors: [String!]
  pages: Int
  publisher: String
  published: String
  thumbnail: String
}
