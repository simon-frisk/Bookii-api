type Query {
  signin(email: String!, password: String!): String!
  user(userId: ID): User
}

type Mutation {
  signup(user: UserInput!): String!
}

type User {
  _id: ID!
  email: String!
  name: String!
}

input UserInput {
  email: String!
  name: String!
  password: String!
}
