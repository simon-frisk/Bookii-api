type Query {
  signin(email: String!, password: String!): String
  hello: String!
}

type Mutation {
  signup(user: UserInput!): String!
}

type User {
  _id: String!
  email: String!
  name: String!
}

input UserInput {
  email: String!
  name: String!
  password: String!
}
